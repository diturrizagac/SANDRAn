<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:dist="http://schemas.android.com/apk/distribution"
        package="com.example.scoreregisterapp">

    <dist:module dist:instant="true" />

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.hardware.camera" />
    <uses-permission android:name="android.hardware.camera.autofocus" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
            android:allowBackup="true"
            android:hardwareAccelerated="false"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:largeHeap="true"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme">
        <activity android:name=".presentation.GradesListActivity"></activity>
        <activity android:name=".presentation.CreateLessonActivity" />
        <activity android:name=".presentation.UserCoursesActivity" />
        <activity android:name=".presentation.UserProfileActivity" />
        <activity android:name=".presentation.StudentListActivity" />
        <activity android:name=".presentation.qr.QrGenerateActivity" />
        <activity android:name=".presentation.StudentSummaryActivity" />
        <activity android:name=".presentation.SelectScoreActivity" />
        <activity android:name=".presentation.SaveStudentScoreActivity" />
        <activity android:name=".presentation.qr.QrResultActivity" />
        <activity android:name=".presentation.qr.QrScanActivity" />
        <activity android:name=".presentation.UserHomeActivity" />
        <activity android:name=".presentation.AuthenticationActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".presentation.custom.DashboardActivity">
        </activity>

    </application>

</manifest>